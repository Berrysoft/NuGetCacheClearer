# NuGetCacheClearer
Clear NuGet cache, preserving the newest packages.

## Usage
``` bash
# clear directly
$ ./NuGetCacheClearer
# clear with verbose
$ ./NuGetCacheClearer -v
# show what will be cleared
$ ./NuGetCacheClearer --dry-run
```
